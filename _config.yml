title: Hubble Docs
email: help@hubble.vote
description: >-
  Hubble user documentation
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://docs.hubble.vote" # the base hostname & protocol for your site, e.g. http://example.com

permalink: pretty
remote_theme: just-the-docs/just-the-docs
plugins:
  - jekyll-feed

collections:
  docs:
    permalink: "/:path"
    output: true

just_the_docs:
  search_enabled: true
  heading_anchors: true
  collections:
    docs:
      name: Documentation
      nav_exclude: false
      search_exclude: false

aux_links:
  "Docs on GitHub":
    - "//github.com/vote-hubble/docs"
aux_links_new_tab: true

back_to_top: true
back_to_top_text: "Back to top"
color_scheme: dark
footer_content: "&copy; 2023 HBLE, Inc."

# Footer last edited timestamp
last_edit_timestamp: true # show or hide edit time - page must have `last_modified_date` defined in the frontmatter
last_edit_time_format: "%b %e %Y at %I:%M %p" # uses ruby's time format: https://ruby-doc.org/stdlib-2.7.0/libdoc/time/rdoc/Time.html

# Footer "Edit this page on GitHub" link text
gh_edit_link: true # show or hide edit this page link
gh_edit_link_text: "Edit this page on GitHub"
gh_edit_repository: "https://github.com/vote-hubble/docs" # the github URL for your repo
gh_edit_branch: "main" # the branch that your docs is served from
# gh_edit_source: docs # the source that your files originate from
gh_edit_view_mode: "tree" # "tree" or "edit" if you want the user to jump into the editor immediately

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
